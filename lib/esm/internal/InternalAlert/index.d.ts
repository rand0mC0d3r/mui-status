/// <reference types="react" />
export default function ({ uniqueId, actions, source, severity, message, code }: {
    uniqueId: string;
    actions?: any;
    source?: string;
    severity: any;
    message: string;
    code: string;
}): JSX.Element;
